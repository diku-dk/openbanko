language: c

before_install:
  - sudo add-apt-repository --yes ppa:hansjorg/rust
  - sudo apt-get update -qq
  - sudo apt-get install -qq rust-nightly cargo-nightly

install:
  - (cd bankopladeformat; make)
  - (cd bankopak; make)
  - (cd bankoviser; cargo build)

script:
  - (cd bankopak; make test)
